import styled from "styled-components";

const Header = styled.div`
    width: 100%;
    height: 70px;
    background: #202020;
`

export const MainHeader = () => {
    return(
        <Header>
            
        </Header>
    )
}